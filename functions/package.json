{
  "name": "youneka-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions,firestore:rules",
    "lint": "node --check index.js",
    "serve": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^7.2.2",
    "googleapis": "^150.0.1"
  }
}
